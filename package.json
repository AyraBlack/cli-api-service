{
  "name": "cli-api-service",
  "version": "1.0.0",
  "description": "HTTP API for yt-dlp, Puppeteer, FFmpeg, and YouTube via ytdl-core",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^23.11.0", // Keep base puppeteer
    "ytdl-core": "^4.11.1",
    "puppeteer-extra": "^3.3.6", // <-- ADD THIS LINE
    "puppeteer-extra-plugin-stealth": "^2.11.2" // <-- ADD THIS LINE
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
